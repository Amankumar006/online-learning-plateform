
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    function isAdmin() {
      return get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }

    function isParticipant(roomId) {
      return exists(/databases/$(database)/documents/studyRooms/$(roomId)/participants/$(request.auth.uid));
    }
    
    function isRoomOwner(roomId) {
        return get(/databases/$(database)/documents/studyRooms/$(roomId)).data.ownerId == request.auth.uid;
    }

    match /users/{userId} {
      allow read: if request.auth.uid == userId || isAdmin();
      allow update: if request.auth.uid == userId || isAdmin();
      allow create: if request.auth.uid == userId;
      allow delete: if false;
    }

    match /lessons/{lessonId} {
      allow read: if request.auth != null;
      allow create, update, delete: if isAdmin();
    }
    
    match /exercises/{exerciseId} {
      allow read: if request.auth != null && (isAdmin() || resource.data.isCustom == false || resource.data.userId == request.auth.uid);
      allow create: if request.auth != null && (isAdmin() || (request.resource.data.isCustom == true && request.resource.data.userId == request.auth.uid));
      allow update: if isAdmin();
      allow delete: if request.auth != null && (isAdmin() || (get(/databases/$(database)/documents/exercises/$(exerciseId)).data.userId == request.auth.uid && get(/databases/$(database)/documents/exercises/$(exerciseId)).data.isCustom == true));
    }

    match /exerciseResponses/{responseId} {
      allow read: if request.auth != null && (resource.data.userId == request.auth.uid || isAdmin());
      allow write: if request.auth != null && request.resource.data.userId == request.auth.uid;
    }

    match /announcements/{announcementId} {
      allow read: if request.auth != null;
      allow write: if isAdmin();
    }

    match /lessonRequests/{requestId} {
      allow read, update, delete: if isAdmin();
      allow create: if request.auth != null && request.resource.data.userId == request.auth.uid;
    }

    match /emailQueue/{emailId} {
      allow create: if isAdmin();
      allow read, update, delete: if false;
    }
    
    match /studyRooms/{roomId} {
      // LIST: A user can list rooms that are public OR that they are a participant in.
      // This rule aligns with the queries in `getStudyRoomsForUser`.
      allow list: if request.auth != null && (request.query.where.isPublic == true || request.auth.uid in request.query.where.participantIds);
      
      // GET: A user can get a specific room if it's public OR they are a participant.
      allow get: if request.auth != null && (resource.data.isPublic == true || request.auth.uid in resource.data.participantIds);

      // CREATE: A user can create a room if they set themselves as the owner.
      allow create: if request.auth != null && request.resource.data.ownerId == request.auth.uid;
      
      // UPDATE: A participant can update the room state. Only the owner can end the session.
      allow update: if request.auth != null && (isParticipant(roomId) && request.resource.data.status == resource.data.status || isRoomOwner(roomId));

      // DELETE: Only the owner can delete a room.
      allow delete: if request.auth != null && isRoomOwner(roomId);
      
      match /participants/{userId} {
        // Read: Any participant can see the list of other participants.
        allow read: if isParticipant(roomId);
        // Write: A user can only add/update/delete their own participant document.
        allow write: if request.auth.uid == userId;
      }
      
      match /messages/{messageId} {
        // Read: Any participant can read messages.
        allow read: if isParticipant(roomId);
        // Create: A participant can send a message if they are the sender and the room is active.
        allow create: if isParticipant(roomId) && request.resource.data.userId == request.auth.uid && get(/databases/$(database)/documents/studyRooms/$(roomId)).data.status == 'active';
        allow update, delete: if false;
      }
      
      match /resources/{resourceId} {
        // Read: Any participant can read resources.
        allow read: if isParticipant(roomId);
        // Create: A participant can add a resource if they are the one adding it and the room is active.
        allow create: if isParticipant(roomId) && request.resource.data.addedByUserId == request.auth.uid && get(/databases/$(database)/documents/studyRooms/$(roomId)).data.status == 'active';
        // Delete: The person who added the resource or the room owner can delete it.
        allow delete: if request.auth != null && (isRoomOwner(roomId) || get(/databases/$(database)/documents/studyRooms/$(roomId)/resources/$(resourceId)).data.addedByUserId == request.auth.uid);
        allow update: if false;
      }
    }
  }
}
